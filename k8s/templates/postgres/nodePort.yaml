kind: Service
apiVersion: v1
metadata:
  name: postgres-nodeport-service
spec:
  type: NodePort
  selector:
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30001
      name: sql-port
