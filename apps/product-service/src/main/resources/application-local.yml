spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      platform: postgres

logging:
  level:
    root: info
    eshop.api: debug
grpc:
  server:
    port: 9093